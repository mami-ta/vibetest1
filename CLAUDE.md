# CLAUDE.md

Claude Code が**毎回の作業の最初に必ず読む**ファイルです。
ここに書いたことが、以降すべての作業の前提になります。

> **見た目のことは [`DESIGN.md`](DESIGN.md) に分けてあります。**
> 画面を作る・直すときは、必ずそちらも読んでください。

技術のルールだけではありません。**一番上に「誰の、どんな課題か」を書く欄**があります。
この欄が空のままだと Claude は一般論で作ります。埋まっていると、その人のために作ります。

> **使い方**: まず `docs/01_customer.md` に**3行だけ**書いて（5分）、`/kickoff` を実行してください。
> 残りは Claude Code が**仮説**で埋め、このファイルの1〜4章が揃います。
>
> 📄 **3行から、どう埋まるか → [`samples/CLAUDE.sample.md`](samples/CLAUDE.sample.md)（記入例）**

---

## 1. このプロダクトは誰のためか

<!-- 実名で挙げられる顧客。属性ではなく人。 -->

- **顧客（実名で3人）**:
  1. 子どもの大学受験のスケジュールを紙で一覧にしている主婦の高橋麻美さん
  2. 🔮 高橋さんと同じ学年の子を持つ、塾の保護者会などで知り合う保護者
  3. 🔮 同上（受験情報サイトの見づらさに困っている保護者）
- **その人に、他の人より会いやすい理由**: 🔮 未確認
  → 確かめ方:「高橋さんとはどういうきっかけで知り合いましたか」

> ⚠ 「30代の共働き世帯」「中小企業の経営者」はNG。**顔が浮かぶ人**まで下ろす。
> 3人書けないなら、題材そのものを変えたほうが速い。

---

## 2. 解く課題

<!-- その人が今どうやっているか。何に時間と費用を使っているか。 -->

- **困っていること**: 入試要項が試験の種類ごとに順次公開されるので、1か月に1回は3〜5校の受験生向けサイト情報を確認しなければならない。試験区分が細かく分かれすぎていて、何と何が併願できるのかがよくわからない。試験で必要とされる受験科目を入試要項から探さないといけない。願書の提出や試験日、受験科目、合格発表、支払期日を家族で共有したいが、カレンダーアプリではうまくできないので、紙に書き出して一覧にしている。一般公開されている管理アプリはあるが、余計な学校のプロモーションが入ってくるので煩わしくて使いたくない。
- **今その人はどうやっているか**: 紙に書き出して一覧にしている
- **かかっている時間 / 費用**: 🔮 1校・1試験区分の確認に約30分、月3〜5校分で月2〜3時間、家族用に清書し直す時間が月1時間ほど。合計で月3〜4時間程度。外注（受験コンサル等）だと1回5,000〜1万円程度
  → 確かめ方:「今の一覧表を作るのに、月にだいたい何時間くらいかけていますか？」
- **なぜ今まで誰も解決していないのか**:
  - [x] データが散っている　[ ] 市場が小さい　[ ] 量が多すぎる
  - [ ] 判断が非定型　[ ] n=1すぎる
  🔮 各校が試験区分ごとに別々のタイミング・別ページで要項を公開するため情報が1か所に集まっておらず、かつ志望校の組み合わせが各家庭ごとに違う（n=1に近い）ため汎用データベース化しにくい
  → 確かめ方:「今使っている受験情報アプリやサイトはありますか？なぜ使うのをやめた／使わなかったのですか？」
- **このサイトで最初に成立させたいこと（1つだけ）**: どの学校・どの試験区分を受けるかを選択すると、出願日・試験日・合格発表日・入学手続き期間が一覧で見られること

---

## 3. やらないこと

<!-- 今回入れない機能。ここを書くと、余計なものが増えない。 -->

- 学校サイトの入試要項を自動収集・スクレイピングして最新化する機能（媒体化はしない。情報は麻美さんが自分で確認して手入力する）
- 家族間のリアルタイム共有・通知（LINE/メール通知、複数ユーザーでのログイン共有）
- 出願書類のアップロードや提出代行、支払いの決済機能

> ⚠ **この欄が空だと、Claude は良かれと思って機能を足します。**
> 作りたくなるが今回はやらないことを、先に3つ書いてください。

---

## 4. 出口 ── 作る前に確認する3つ

作れても、ここが埋まらないと事業になりません。3つともコードを1行も書かずに数日で確認できます。

### ① 誰が払うか

- **払う人**: 🔮 高橋麻美さん本人（使う人と払う人が同じと思われる）
- **その相手は、この種のものに金を出す商習慣があるか**: 🔮 受験関連（模試代・塾代・受験情報誌・スタディサプリ等）にお金を払う商習慣は家庭に根付いており、抵抗は少ないと考えられる
  → 確かめ方:「こういうツールが月500円くらいだったら使ってみたいと思いますか？」

> 単価が高く見えても、**そもそも予算を張る文化がない相手**は出口にならない。

### ② どう届くか

- **その市場が「実際に使っている」経路**: 🔮 塾の保護者会・PTA・ママ友ネットワーク・受験情報系のSNSコミュニティといった「下」の経路（紹介・口コミ）だと思われる
  → 確かめ方:「もしこれが便利だったら、誰に一番に教えたいと思いますか？」

| | 経路 | AI時代の状況 |
|---|---|---|
| 上 | 検索・広告・コールドDM・SNS | **獲得コストが上昇中**（供給が10倍、注意の総量は一定） |
| 下 | 紹介・口コミ・既存顧客・業界内の直接接触 | **変わらない。ここが取り分** |

> ⚠ 自分が使いたい経路ではなく、**顧客がすでに使っている経路**を書く。

### ③ 受け取れるか（決済・審査・規制）

- **確認したか**: いいえ
- **確認した内容**: 🔮 学習・スケジュール管理ツールは要確認業種（投資/金融・医療/健康・法律相談等）には該当しないと考えられる。ただし学校サイトの入試要項をそのまま転記・引用する場合は著作権・利用規約の範囲を確認したほうが安全
  → 確かめ方: 利用しようとしている学校の募集要項ページの利用規約・著作権表記を確認する

> 要確認: 投資・金融／医療・健康／サプリ・美容の効能／法律相談／暗号資産／ギャンブル／アダルト／情報商材・副業

### 競合には必ず「DIY」を入れる

- **既存の競合**: 河合塾・駿台などの入試情報サイト、Classi、みんなの大学情報、汎用カレンダーアプリ（Googleカレンダー等）
- **DIY競合**（顧客が自分でAIに作らせる）: Excel/Googleスプレッドシートを自作する、ChatGPTに管理表のひな形を作らせる
- **選ばれる理由**: 🔮「時間」＝ 複数校のサイトを巡回して情報を探す時間を、志望校・試験区分を選ぶだけの操作に減らせる。「埋め込み」＝ 今すでにある「紙の一覧表を見る」という家庭内の日常フローに、そのまま置き換わって入り込める
  → 確かめ方:「今の紙の一覧表は、1日にどれくらいの頻度で見返しますか？」

**「選ばれる理由」を機能で書かない。** 機能は数週間で追いつかれます。書いてよいのは4つだけ。

| データ | 責任 | 時間 | 埋め込み |
|---|---|---|---|
| 自分しか持っていない情報 | 結果に責任を取る | 相手の時間を実際に減らす | 業務フローの中に入っている |

---

## 5. 技術ルール

| 何 | 使うもの |
|---|---|
| フレームワーク | Next.js (App Router) |
| 言語 | TypeScript（`strict` 有効） |
| スタイル | **プレーンCSS**（`app/globals.css` の変数を使う）。見た目の判断は **`DESIGN.md`** に従う |
| 公開 | GitHub → Vercel（PRをマージすると自動で公開される） |

- **新しいライブラリを勝手に追加しない。** 必要なら理由と代替案を説明して許可を取る
- CSSフレームワーク（Tailwind等）は導入しない
- 色・余白・文字サイズは**必ず `app/globals.css` の変数を使う**
  - 良い: `color: var(--color-accent);`　／　悪い: `color: #1f5fd6;`
- 見た目を大きく変えたいと言われたら、個別ファイルではなく**変数の値**を変える
- 設定ファイル（`next.config.ts` / `tsconfig.json` / `package.json`）は頼まれない限り触らない

---

## 5-2. 「動くツール」を作るときの技術ルール

このプロジェクトのゴールは、説明ページ（LP）ではなく **触ると動くツール** です。

### ⛔ 絶対にやらないこと

1. **LP（宣伝ページ）を作らない。**
   キャッチコピー・課題の説明セクション・「無料で始める」等のCTA・営業文は作りません。
   作るのは、**左メニュー → 現在の状況 → 検索・絞り込み → データ一覧** という業務画面です。
2. **`docs/` の中身を画面に出さない。**
   「やらないこと」「🔮の仮説」「出口の3点」「競合分析」は**社内向けのメモ**です。
   これらが画面に出ていたら間違いです。要件定義書の中で使い切ってください。

詳しくは **`DESIGN.md` の 0章** を読んでください。

| | |
|---|---|
| ファイル | **`app/page.tsx` の1枚**で完結させる |
| 状態 | React の `useState`。ファイル先頭に `"use client"` |
| 保存 | **`localStorage`**。`useEffect` の中でだけ読み書きする |
| 画面 | 左メニューの**3画面（一覧／新規登録／設定）**。ルーティングは増やさない |
| データ項目 | **5つまで。**多いと、その場で入力して試せなくなる |
| 作るもの | **1種類のデータの管理ツール**（登録して・探して・状態を変える） |
| 作らないもの | メディア・マッチング・決済・ログイン・お客さん側からの入力（第2〜3回で扱う） |

### localStorage は必ずこの形で書く

`useEffect` の外で `localStorage` を読むと、**ビルドが落ちます**（サーバー側に `localStorage` が無いため）。

```tsx
"use client";
import { useState, useEffect } from "react";

const KEY = "xxx-data";

export default function Home() {
  const [items, setItems] = useState<Item[]>([]);
  const [loaded, setLoaded] = useState(false);

  useEffect(() => {                    // 読み込み
    try {
      const raw = localStorage.getItem(KEY);
      setItems(raw ? JSON.parse(raw) : INITIAL);
    } catch { setItems(INITIAL); }
    setLoaded(true);
  }, []);

  useEffect(() => {                    // 保存（読み込み前に走らせない）
    if (!loaded) return;
    localStorage.setItem(KEY, JSON.stringify(items));
  }, [items, loaded]);
}
```

> 実装例（ビルド・動作確認済み）: `samples/page.tool-sample.tsx`

---

## 6. 指示の受け方と、進め方

### ユーザーの指示は「4つの型」で受け取る

| | 何を | 例 |
|---|---|---|
| **① 対象** | どのファイルを触るのか | `app/page.tsx` のトップページを |
| **② やること** | 1回の指示に1つだけ | `docs/01_customer.md` の内容に合わせて書き換えて |
| **③ 条件** | 守ってほしいこと | 見出し1つ、説明3行、CTAボタン1つ |
| **④ 禁止** | やらないでほしいこと | 新しいライブラリは追加しない／色は既存の変数を使う |

**①②が読み取れない指示が来たら、作り始める前に聞き返してください。**
「いい感じに」「モダンに」だけで進めない。色・文言・数を確認する。

### 仕上げは「追加 → 修正 → 削除」の順で、1回1つ

1. **追加** — 足りない要素を1つ入れる
2. **修正** — 文言と色を直す
3. **削除** — 不要な部分を消す

> 削除を最後にするのは、消してから足すと**同じものをまた作らせる**ことになるから。
> 3つをまとめて実行しない。**どれが原因で崩れたか追えなくなります。**

### 進め方

- **日本語で応答する。** 専門用語には必ず一言で言い換えを添える
- **一度に1つのことだけやる。** 「ついでにあれも直しておきました」はしない
- 作業の前に、**何をどのファイルにやるつもりか**を1〜3行で先に伝える
- **差分で直す。** 直したい箇所以外を作り直さない
- 動くものを先に出す。完璧な設計より、まず画面に出る状態を優先する
- エラーが出たら、**原因を特定してから直す**

---

## 7. 仮説（🔮）の扱い ── 一番大事なルール

このプロジェクトでは、**事実と仮説を必ず区別します。**

| 表記 | 意味 | 誰が書いたか |
|---|---|---|
| （マークなし） | **確かめた事実** | ユーザーが顧客に会って聞いた |
| **🔮** | **まだ確かめていない仮説** | Claude が推測で埋めた |

### 埋まっていない項目は、質問せずに仮説で埋める

ユーザーは今すぐ顧客に会えません。**聞き返して止めるより、仮説を置いて前に進めるほうが価値があります。**

- 空欄を見つけたら、**もっともらしい仮説を立てて 🔮 を付けて埋める**
- 断定しない。「〜と思われる」「〜の可能性が高い」で書く
- 🔮 の項目には、**その顧客に何と聞けば確かめられるか**を1行添える

### ただし、仮説を事実として扱わない

- 🔮 が付いた項目を根拠に、**実績・数字・お客様の声をページに書かない**
- ユーザーが 🔮 の内容を前提に大きく作り込もうとしたら、**一度だけ**こう伝える
  > 「その項目はまだ 🔮（仮説）です。先に確かめたほうが安全ですが、進めますか？」
- 伝えたうえでユーザーが進めると言ったら、**そのまま従ってください。**二度は言わない

### 特に重い2つ

**① 誰が払うか** と **② どう届くか** が 🔮 のままの場合、
作り込む前にそれを伝えてください。特に②を外すと、**量では取り返せません。**

---

## 8. やってはいけないこと

- **APIキー・パスワード・個人情報をコードに書かない。** 必要なら `.env.local`（Gitに含まれない）を使う
- 頼まれていないファイルを削除しない。消す必要があるときは先に確認を取る
- **3章「やらないこと」に書かれているものを実装しない。** 必要だと思ったら提案だけして止まる
- **実在しない数字・実績・お客様の声を作らない**（「導入企業500社」を勝手に書かない）
- **🔮（仮説）の内容を、事実としてページに書かない。** 仮説はドキュメントの中だけに置く
- 動作を確認していないのに「完成しました」と言わない
- **AIのAPIを使う機能を足すときは、1回あたりのコストと上限設計をセットで提案する。**
  上限なしで実装しない

---

## 9. ファイルの地図と、公開の仕組み

```
.
├─ CLAUDE.md              ← このファイル（毎回読まれる）
├─ DESIGN.md              ← 見た目の決めごと（画面を作るとき必ず読む）
├─ app/
│  ├─ layout.tsx          ← 全ページ共通の枠。サイトのタイトル・説明
│  ├─ page.tsx            ← トップページの中身
│  └─ globals.css         ← 色・余白・文字サイズの定義
├─ docs/
│  ├─ 01_customer.md      ← 3行＋仮説（1〜2章のもとになる）
│  ├─ 02_onepager.md      ← 何を作るかの1枚仕様
│  └─ 03_spec.md          ← 要件定義書（/spec が埋める）
├─ samples/               ← 記入例と、動くツールの実装例
└─ .claude/commands/      ← /kickoff /spec /build /customer /gtm /plan /check
                            ※ これはただのMDファイル。他のPJで使うならコピーするか、
                              中身をそのまま貼れば同じ動きになる
```

```
Claude が変更 → 差分を確認 → Create PR → GitHub でマージ → Vercel が自動で再公開
```

- **マージするまで、本番サイトは変わりません。** 安心して試してよい
- PRを作る前に、変更内容を**箇条書き3行以内**で説明する
- 1回の指示 = 1つのPR。まとめて出さない

---

## 10. 迷ったときの優先順位

1. **1〜3章（誰のため・解く課題・やらないこと）に沿っているか**
2. **4章（出口）が崩れないか**
3. 動くか
4. **`DESIGN.md` に反していないか**
5. あとで人間が読めるか

> 作るコストは下がった。売るコストは上がった。
> **だから、作る前に「誰のため」と「出口」を確認する。** それがこのファイルの一番の役目です。
